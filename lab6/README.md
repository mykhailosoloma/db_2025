# Лабораторна робота №6: Міграції баз даних з Prisma ORM

Цей проект є результатом виконання лабораторної роботи №6, присвяченої вивченню **Prisma ORM** (Object-Relational Mapping). Основна мета — навчитися керувати схемою бази даних PostgreSQL, виконувати міграції та взаємодіяти з даними через JavaScript.

## 📋 Завдання та Реалізовані Зміни

У ході роботи було виконано перехід від ручного написання SQL-запитів (як у попередніх роботах) до підходу **Code-First** за допомогою Prisma.

Були реалізовані наступні зміни в схемі бази даних:

1.  **Інтроспекція (Introspection):**
    * Зчитано існуючу структуру БД (створену в Лаб 5) командою `db pull`.
    * Згенеровано файл `schema.prisma` на основі існуючих таблиць (`users`, `courses`, `lessons` тощо).

2.  **Створення нової таблиці `Material`:**
    * Додано модель для зберігання додаткових матеріалів до уроків.
    * Поля: `material_id`, `title`, `url`.
    * Встановлено зв'язок `One-to-Many` з таблицею `lessons`.

3.  **Модифікація таблиці `users`:**
    * Додано нове поле `phone` (тип `String?`) для зберігання номера телефону користувача.

4.  **Рефакторинг таблиці `courses`:**
    * Видалено поле `description`.
    * Додано нове поле `summary`.
    * Ці зміни були застосовані через міграцію.

---

## 🛠 Технологічний стек

* **Node.js**: Середовище виконання.
* **Prisma ORM** (v5.22.0): Інструмент для роботи з БД.
* **PostgreSQL**: Реляційна база даних.
* **Visual Studio Code**: Середовище розробки.

